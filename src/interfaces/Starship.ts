export interface Starship {
  id: number;
  name: string;
  model: string;
  starshipClass: string;
}

export interface StarshipResponseData {
  id: number;
  name: string;
  model: string;
  starship_class: string;
}
